name: Approved Apollo Coverage Crawl

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/acquisition-apollo-crawl-once.yml

permissions:
  contents: read
  statuses: read

concurrency:
  group: approved-apollo-coverage-crawl
  cancel-in-progress: false

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      BASE_URL: https://sdr.dashboardtalentera.tech
      ACQUISITION_OWNER_TOKEN: ${{ secrets.ACQUISITION_OWNER_TOKEN }}
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Validate authorization
        run: |
          set -euo pipefail
          test -n "$ACQUISITION_OWNER_TOKEN" || { echo "::error::ACQUISITION_OWNER_TOKEN is missing."; exit 1; }

      - name: Wait for this exact revision to finish Hostinger deployment
        run: |
          set -euo pipefail
          API="https://api.github.com/repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/status"
          for attempt in $(seq 1 150); do
            curl -sS --connect-timeout 5 --max-time 15 \
              -H "Authorization: Bearer $GH_TOKEN" -H 'Accept: application/vnd.github+json' \
              "$API" -o /tmp/status.json || true
            state=$(jq -r '[.statuses[]? | select(.context=="hostinger/deploy")][0].state // "pending"' /tmp/status.json 2>/dev/null || echo pending)
            echo "Hostinger deploy status: $state (${attempt}/150)"
            if [ "$state" = success ]; then exit 0; fi
            if [ "$state" = failure ] || [ "$state" = error ]; then
              echo "::error::Hostinger deployment failed; refusing to spend Apollo credits."
              exit 1
            fi
            sleep 5
          done
          echo "::error::Timed out waiting for Hostinger deployment; no Apollo crawl started."
          exit 1

      - name: Crawl Apollo pages 2 through 89 exactly once
        run: |
          set -euo pipefail
          PROGRESS_URL="$BASE_URL/api/acquisition/coverage-progress"
          CRAWL_URL="$BASE_URL/api/acquisition/coverage-crawl-page"

          curl -sS --connect-timeout 10 --max-time 30 "$PROGRESS_URL" -o /tmp/progress.json || echo '{"completedPages":[]}' >/tmp/progress.json
          completed=$(jq -c '.completedPages // []' /tmp/progress.json 2>/dev/null || echo '[]')
          echo "Existing persistent checkpoints: $completed"

          pages_spent=0
          pages_skipped=0
          companies_stored=0
          for page in $(seq 2 89); do
            if echo "$completed" | jq -e --argjson page "$page" 'index($page) != null' >/dev/null; then
              echo "Page $page already checkpointed; skipping without credit spend."
              pages_skipped=$((pages_skipped + 1))
              continue
            fi

            echo "Apollo page $page/89: authorized 1 organization-search credit."
            http=$(curl -sS --connect-timeout 10 --max-time 90 \
              -X POST "$CRAWL_URL" \
              -H "x-acquisition-owner-token: $ACQUISITION_OWNER_TOKEN" \
              -H 'Content-Type: application/json' \
              --data "{\"page\":$page}" \
              -o /tmp/page.json -w '%{http_code}' || true)

            if [ "$http" != 200 ]; then
              echo "::error::Apollo page $page failed with HTTP ${http:-000}. Stopping immediately; this workflow never retries a failed page automatically."
              jq '{error,crawlPage,startPage,lastPage,stored,checkpointed,checkpointWarning}' /tmp/page.json 2>/dev/null || true
              exit 1
            fi

            stored=$(jq -r '.stored // 0' /tmp/page.json)
            checkpointed=$(jq -r '.checkpointed // false' /tmp/page.json)
            warning=$(jq -r '.checkpointWarning // ""' /tmp/page.json)
            echo "Page $page stored=$stored checkpointed=$checkpointed"
            if [ "$checkpointed" != true ]; then
              echo "::warning::Page $page was stored but checkpoint write warned: $warning"
            fi
            pages_spent=$((pages_spent + 1))
            companies_stored=$((companies_stored + stored))
            completed=$(echo "$completed" | jq -c --argjson page "$page" '. + [$page] | unique | sort')
            sleep 1
          done

          echo "Apollo crawl complete: new_pages=$pages_spent already_checkpointed=$pages_skipped stored_rows=$companies_stored SignalHire_contact_credits=0"
          {
            echo "## Apollo Coverage Crawl"
            echo "- Newly executed Apollo pages: $pages_spent"
            echo "- Already checkpointed pages skipped: $pages_skipped"
            echo "- Stored/upserted account rows reported: $companies_stored"
            echo "- SignalHire contact credits used: 0"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Verify persistent coverage status
        run: |
          set -euo pipefail
          curl -sS --connect-timeout 10 --max-time 30 "$BASE_URL/api/acquisition/coverage-status" -o /tmp/final.json
          jq '{stored:.ledger.stored, eligible:.ledger.eligible, completedPages:.probe.completedPageCount, totalPages:.probe.totalPages, pageCoveragePercent:.probe.pageCoveragePercent, remaining:.probe.estimatedAdditionalSearchCreditsToFinish}' /tmp/final.json
          completed=$(jq -r '.probe.completedPageCount // 0' /tmp/final.json)
          total=$(jq -r '.probe.totalPages // 89' /tmp/final.json)
          [ "$completed" -eq "$total" ] || { echo "::error::Coverage checkpoint is ${completed}/${total}, expected complete."; exit 1; }
