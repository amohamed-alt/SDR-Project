name: Tier 1 Saudi Apollo People Search Once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled intentionally
        run: |
          echo "This one-off workflow is disabled intentionally."
          echo "Do not read or write the Talentera dashboard for the Tier 1 Saudi Apollo export task."
          echo "Use Apollo API only, with an explicit private input channel, and no enrichment or phone reveal calls."
