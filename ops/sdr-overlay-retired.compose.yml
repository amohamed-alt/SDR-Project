name: sdr-auth-hotfix

services:
  sdr-dashboard-public:
    image: alpine:3.21
    restart: "no"
    mem_limit: "16m"
    cpus: 0.05
    pids_limit: 16
    labels:
      - "traefik.enable=false"
    command:
      - /bin/sh
      - -c
      - |
        echo "Temporary SDR auth overlay retired"
        exit 0
