services:
  sdr-dashboard:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:3010:3000"
    volumes:
      - sdr-google-data:/app/data

volumes:
  sdr-google-data:
