# Companion v1.5.3 single-queue behavior

- Sales Navigator is the primary discovery source.
- `Capture FULL search` is the primary Companion action.
- Quick 25/50 extracts are testing helpers only, but they now persist into the same Search History / Persistent Prospecting Queue.
- `/salesnav-prospecting` is setup-only and never auto-enriches imported leads.
- SignalHire contact reveal remains manual in the persistent queue after HubSpot gating.
